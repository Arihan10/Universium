using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun; 

public class ShipController : MonoBehaviour
{
    /* public Camera cam;

    // public GameObject planet; 
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void StartPiloting() {
        GetComponent<ShipMovement>().StartPiloting(); 
        cam.gameObject.SetActive(true); 
    }

    public void StopPiloting() {
        GetComponent<ShipMovement>().StopPiloting();
        GameObject.Find("PlayerController(Clone)").GetComponent<FirstPersonController>().planet = GetComponent<ShipMovement>().planet; 
        cam.gameObject.SetActive(false); 
    }
    */
}
